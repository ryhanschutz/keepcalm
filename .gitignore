node_modules/
dist_build/
KeepCalm-win32-x64/
*.log
build_err.log
build_log.txt
build_zip.log
build_err_utf8.log
build_zip_utf8.log
.DS_Store
Thumbs.db

# ── Privacidade ──────────────────────────────────────────────────────────────
# Dados locais de sessão (ao lado do .exe no modo portátil) — nunca commitar
KeepCalm_Data/

# Credenciais reais para desenvolvimento local — NUNCA commitar
# Copie config.secret.template.js para config.secret.js e preencha suas credenciais
config.secret.js

# Arquivo de variáveis de ambiente local
.env
.env.local
.env.*.local
